# update-delivery-suggester

定例MTG後のアポ打診・新機能デリバリーを支援するスキル。
メディカルフォース（電子カルテ）のアップデート情報と、SalesforceのMTGメモを照合し、
確定／候補／対象外の3段階で整理する。最終的なデリバリー文面まで出力する。

---

## インプット

### 必須（3つ）

1. **アップデート情報.md**（メディカルフォース）
   - Project Knowledgeに格納済みの最新版を参照する（毎回アップロードする必要はない）
   - 正本はエンジニアが管理するGoogle Drive上の「アップデート情報.md」。Project Knowledgeへは月次または新機能リリース時に手動同期される
   - 対象期間：直近の定例MTGから遡って3ヶ月程度を目安に絞り込む
2. **SalesforceのMTGメモ**（スクショ、またはClaude Code環境ならSOQLでの直接取得）
   - 以下のフィールドが写っていること
     - 議事録サマリ（決定事項・ペンディング事項・ネクストアクション）
     - 先方課題
     - 持ち帰り事項（要望含む）
3. **Salesforceの契約状況**（Account・Opportunity）
   - Claude Code環境（SOQLが叩ける場合）で確認する。詳細は次章「契約状況の確認（スコープ確認）」を参照
   - claude.aiプロジェクト等でSOQLが叩けない場合は、この章はスキップしてよい（Phase1運用＝MTGメモのみでの照合に戻る）

### 入力フォーマット

```
顧客名（クリニック名）
SF：https://xxxxx（SalesforceのレコードURL）
このデータからお願い
```

---

## 契約状況の確認（スコープ確認）

MTGメモだけでは「導入検討中」と書かれていても、実は**MTG後に受注済みで、単に利用開始日がまだ先**というケースがある。逆に、契約していない機能カテゴリのアップデートを案内してしまうと的外れになる。このズレを防ぐため、マッチング処理に入る前に以下を確認する。

### 0. アップデート情報側に機能カテゴリのタグ付け済みファイル（通称「製本」）がある場合はそちらを使う

CS担当のローカル環境（Claude Code等）に、アップデート情報の各項目へあらかじめ機能カテゴリ・契約前提を
タグ付けした構造化ファイルが用意されている場合、生のアップデート情報.mdやクリーニング済みファイルを
毎回読み直して分類し直す必要はない。そちらを正として、以下のFunction__cとの突き合わせに使う。
（カテゴリ判定はアップデート項目自体に紐づく事実であり、クライアントが変わっても変化しないため、
一度タグ付けしたものは使い回してよい。新しいリリース分は差分だけ追記していく運用を推奨）

### 1. Account.利用機能（`Function__c`）でスコープを絞る

```sql
SELECT Id, Name, Function__c, StartDate__c, Status__c FROM Account WHERE Id = '{AccountId}'
```

- `Function__c`（利用機能）に含まれていない機能カテゴリ（例：保険関連、決済端末関連、広告効果測定、在庫管理など）のアップデートは、そのクリニックには関係ないため、確定・候補・対象外いずれの検討対象からも**そもそも除外する**（対象外リストに理由付きで載せる必要もない、除外して問題ない）
- **注意**：`Function__c`は商談が受注（フェーズが「◯◯アップセル受注」等）になったタイミングで更新される運用になっており、必ずしも「現在実際に利用中」を意味しない。実際に使えるかどうかは下記のOpportunity側の`変更適応日`と本日の日付を突き合わせて判定する

### 2. Account配下のOpportunity履歴を確認する

```sql
SELECT Id, Name, StageName, CloseDate, Field21__c, upsell_feature__c, downsell_function__c
FROM Opportunity WHERE AccountId = '{AccountId}' ORDER BY CloseDate DESC
```

- `StageName`が「◯◯アップセル受注」になっており、`CloseDate`が直近MTG日以降のものがあれば、そのMTGで「ペンディング」「検討中」だった機能が**受注済み**になっている可能性が高い。該当する`upsell_feature__c`を確認し、MTGメモ上のペンディング事項と突き合わせる
- `Field21__c`（変更適応日）が本日より先の日付なら、「受注済み・利用開始待ち」の状態。案内文言では「導入済み」ではなく「◯月から利用開始予定」と正確に区別する
- 過去に`StageName`が「◯◯アップセル失注」になっている機能（例：在庫管理）は、それ以降に別の商談で受注していない限り契約外。`Function__c`に含まれていないことの裏付けとして使う
- 一つのAccountに複数のOpportunityが紐づくことが多いため、`CloseDate DESC`で並べて時系列（受注→失注→再受注など）を把握したうえで判断する

### 運用ルール

- 現時点ではこの確認は**都度SOQLで実施する**（クリニックごとの契約状況スナップショットをファイル保存して使い回す運用は、必要になった時点で改めて検討する。現時点では未着手）
- SOQLの実行はClaude Code環境を想定。usernameOrAliasは`my-org`

---

## マッチング処理（3段階判定）

アップデート情報とMTGメモを照合し、以下の3段階で分類する。

### 確定

根拠が明確なもの。以下のいずれかを満たす場合。

- MTGの先方課題・持ち帰り事項と機能の対応が1対1で明確
- 決定事項として既に検討中の機能に直接関連するアップデート

出力形式：機能名・リリース日・対応する課題・具体的な根拠を記載する。

### 候補

確信度が中〜低のもの。以下のいずれかに該当する場合。

- 間接的に関連するが、確認が必要（導入済みかどうか不明、別機能とのセットが必要など）
- 複数の解釈が成り立つ
- 既に案内済みの可能性がある

出力形式：1件ずつではなく**全件まとめて1回**でCS担当に確認する。
確認後、CS担当の返答に応じて「確定」または「対象外」に移動する。

```
候補（確認お願いします）
1. ○○機能 — △△の理由で関連する可能性。確信度：中
2. ○○機能 — △△で案内済みの可能性。確認してください。確信度：低
…
```

### 対象外

以下のいずれかに該当する場合。

- 該当するアップデートが存在しない
- 守備範囲外（料金・競合比較・人員体制など製品アップデートで解決できない課題）
- 案内済みと確認されたもの
- 候補の確認ステップで「不要」と判断されたもの
- MTGメモ上は「ペンディング」でも、契約状況の確認（Opportunity履歴）で**すでに受注・解決済み**と判明したもの（この場合、関連するアップデートは対象外に落とさず「確定」「候補」側の根拠として使う）

対象外にした理由（守備範囲外の理由も含め）を必ず1行で明記する。

※「契約状況の確認（スコープ確認）」でAccount.利用機能に含まれないと分かった機能カテゴリのアップデートは、この対象外リストにも載せず、検討対象から除外してよい（契約外であることが明確なため、CS担当の目視確認コストをかける必要がない）。

---

## 汎用アップデートの抽出

課題ひも付けの3段階判定とは別に、**先方課題に関係なく、誰でも使えそうなアップデート**も並行して拾い、CS担当に提示する。

- 対象：前提条件（特定プランの契約・複数院運用・保険診療など）がなく、幅広いクリニックに刺さりそうな改善・新機能
- 「契約状況の確認（スコープ確認）」でそのクリニックの利用機能に含まれないと分かったカテゴリは、汎用アップデートの候補からも除外する（例：保険機能を契約していないクリニックに保険関連の汎用アップデートは出さない）
- 出力形式：3段階判定（確定／候補／対象外）とは別枠で、リリース日・機能名を添えて箇条書きで提示する

```
汎用的に使えるアップデート（全クリニック共通・課題ひも付けなし）
- ○○できるようになりました（○/○リリース）
- ○○できるようになりました（○/○リリース）
…
```

---

## 確認プロセス

- **確定・候補は毎回全件、対象外も理由付きで全件、汎用アップデートも全件、CS担当に提示する。1件たりとも省略しない。**
  「確定」だから自動で配信文面に進む、ということはしない。確信度が高いだけで、
  顧客文脈まで踏まえた最終判断ではないため。
- 対象外にも本来デリバリーすべきものが紛れていないか（見逃し／false negative）を、
  CS担当が毎回目視でチェックする。
- 全件の確認が完了するまで、次の「デリバリーパターンの選択」には進まない。
- 「案内済みかどうか」の判断は、SalesforceのMTGメモの記載内容をそのまま信頼する。
  専用の案内履歴ログは別途持たない。
- 「導入検討中かどうか」の判断は、契約状況の確認（Account.利用機能・Opportunity履歴）が取れる場合はそちらを優先する。MTGメモの記載と食い違う場合（例：メモ上はペンディングだが実際は受注済み）は、契約状況側を最新の事実として扱う。

---

## デリバリーパターンの選択

確認が完了したら、確定リストをもとにデリバリーパターンをCS担当が選ぶ。

| パターン | 使う場面 |
|---|---|
| **共有のみ** | 情報提供として送れば十分なとき。アポ不要。 |
| **アポ打診** | MTGの場を設けて話したほうが効果的なとき。複数機能・未導入のアップセル要素があるとき。 |

---

## アウトプット（アポ打診パターン）

アポ打診パターンを選んだ場合、必ず以下の**2ブロックを分けて**出力する。混ぜない。

---

### ブロック1：タイトル化メモ（必須出力）

4列の表形式で出力する。

| アップデート項目 | 言い換えたタイトル | 粒度を決めた理由 | 人の判断推奨箇所 |
|---|---|---|---|
| （機能名・リリース日） | 「（言い換えたタイトル）」 | （理由のみ。判断推奨は右の列に書く） | （CSが判断すべき箇所のみ。なければ空欄） |

#### タイトルの粒度ルール

- **既存導入済み機能の改善** → 機能名・具体的な操作名までタイトルに残してよい
- **未導入機能・アップセル要素を含む機能** → 機能名（プロダクト名・内部呼称）は伏せて、「結果・効果・対象者」で言い換える
- 判断がつかない場合は、抽象側に倒した上で「人の判断推奨箇所」に明記する

#### タイトルの文体ルール（煽らず、淡々と説明する）

機能が何をするかを正確に・淡々と説明することで興味を引く。煽り言葉は使わない。

**NG（煽り・誇張）**
- 「もう自動で気づけます」「だいぶ防げます」
- 「全員じゃなくて狙った人に」のような対比による煽り
- 体言止めでキャッチコピー化（「役務管理、これで解決」など）

**OK（動作をそのまま説明）**
- 「〇〇できるようになりました」「〇〇を防止できます」のように、機能の動作を主語・目的語をそろえて説明する
- 誇張語（もう／だいぶ／意外と／実は）が入っていたら削る
- 削った結果、短すぎる場合は「誰に向けた機能か」を補って具体性で読みやすくする

---

### 派生パターン：担当変更後の初回定例（フル文面）

CS担当の交代直後、顧客との最初の定例を打診する場面では、ブロック1・2をそのまま送るのではなく、
**挨拶・アップデート共有・アポ打診を1通のメッセージにまとめたフル文面**を作成する。
実例は下記「模範解答（大翔会 gift clinic・担当変更後の初回定例）」を参照。

#### 構成

1. 担当変更の挨拶（**1箇所にまとめる**。自己紹介と「連絡した目的」の両方で別々に触れると重複するため、
   「オンラインMTGにて引き継ぎ後のご挨拶も兼ねて」のように1文で済ませる）
2. アップデート共有の趣旨（配信文面ブロックへの導入）
3. 配信文面（ブロック2のルールに従う）。ただし「前回の定例でお伺いした内容に関連する可能性がある」という
   一文を添える場合は、**実際に課題とひも付くものだけにかけ、汎用アップデートを含めない**
   （汎用アップデートは「このほか〜」と別立てで軽く触れる程度に留める）
4. （任意）経営相談への展開：担当変更・関係構築のタイミングだからこそ入れやすい要素として、
   システム活用状況や事業方針・経営課題のヒアリング、売上データ共有などプロダクト改善以上の
   提案機会を作る一文を加えてよい。ただし毎回の必須要素ではなく、CS担当の判断で入れるかどうかを決める
5. 日程候補（プレースホルダーでよい）
6. オンラインMTGツール（Google Meet等）のURL送付案内＋候補日程が合わない場合の代替依頼

---

### ブロック2：配信文面

見出し「▼共有させていただきたいアップデート内容」を付け、タイトルのみ箇条書きで並べる。

- 説明文・補足は入れない（メモ側にのみ書く）
- です/ます体の自然な文章にする（名詞止め・体言止めは避ける）
- 1行1機能

```
▼共有させていただきたいアップデート内容
- 〇〇できるようになりました
- 〇〇を〇〇に応じて変えられるようになりました
- 〇〇を防止できます
```

---

## アウトプット（共有のみパターン）

共有のみパターンを選んだ場合は、**配信文面のみ**を出力する。タイトル化メモは出力しない
（アポ打診が目的ではないため、粒度・伏せ方の人の判断は不要）。

見出し・文体ルールはアポ打診パターンのブロック2と同じ。

```
▼共有させていただきたいアップデート内容
- 〇〇できるようになりました
- 〇〇を〇〇に応じて変えられるようになりました
```

---

## 模範解答（恵比寿ブライトスキンクリニック・2026年1月定例）

### タイトル化メモ（必須出力）

| アップデート項目 | 言い換えたタイトル | 粒度を決めた理由 | 人の判断推奨箇所 |
|---|---|---|---|
| 役務有効期限・消化日数によるMA自動配信（4/7リリース） | 「役務の有効期限が近づいた患者様へ、自動でリマインドを配信できるようになりました」 | 既存「進捗管理」機能の改善であり、課題が具体的なため、機能の中身に近い具体性を残した | クリニックが「進捗管理」機能を利用中かどうかの前提確認が必要 |
| 来院者ランク・タグでのMAシナリオ絞り込み＋LINE連携拡充（4/21リリース） | 「患者様の状況に応じて、配信するメッセージを変えられるようになりました」 | MAは未導入（ペンディング事項）でアップセル要素があるため、機能名（MA）は伏せて抽象度を上げた | LINE連携導入そのものまで提案範囲が広がるため、商談の温度感に応じてCS判断が必要 |
| 施術間隔「0日」設定の活用（既存設定） | 「施術間隔の設定を使うことで、間隔が空いていない予約を防止できます」 | 新機能ではなく既存設定の使い方の話のため、誇張せず機能の動作をそのまま説明する形にした | アップデート共有ではなく設定相談の性質が強いため、アポ打診に混ぜるかサポート対応に分けるかCS判断が必要 |

### 共有させていただきたいアップデート内容

▼共有させていただきたいアップデート内容
- 役務の有効期限が近づいた患者様へ、自動でリマインドを配信できるようになりました
- 患者様の状況に応じて、配信するメッセージを変えられるようになりました
- 施術間隔の設定を使うことで、間隔が空いていない予約を防止できます

---

## 模範解答（レナータクリニック立川院・PoC第1号）

※PoC時の型確認用。タイトル化メモ・配信文面の参照先として使う。

### タイトル化メモ（必須出力）

| アップデート項目 | 言い換えたタイトル | 粒度を決めた理由 | 人の判断推奨箇所 |
|---|---|---|---|
| 役務有効期限・消化日数によるMA自動配信 | 「最後の来院から一定期間が経過した患者様への声かけ、自動でできるように」 | MAは未導入（ペンディング事項）でアップセル要素があるため、機能名（MA）は伏せて抽象度を上げた。導入検討中なので「何ができそうか」の輪郭だけ見せる方が興味を引けると判断 | 　 |
| 来院者ランク・タグでのMAシナリオ絞り込み | 「患者様の状態に応じて、配信メッセージを自動で変えられるように」 | 同上の理由でMAという単語自体を出していない | ここは人の判断で「ランク」「タグ」という言葉まで出すかどうか調整してください |
| コース予約の複数選択 | 「マイページから複数のコースを同時に予約できるようになりました」 | すでに使っている既存機能（持ち帰り事項に上がっている）の改善なので、機能名そのまま具体的に書いて問題ないと判断 | 　 |
| 友達紹介プログラムのポイント付与 | 「友達紹介をきっかけに、紹介者様へポイントが付与される仕組みが追加されました」 | 議事録に紹介施策の言及がなく未導入の可能性が高いため抽象度を上げた | 導入済みなら、もっと具体的にしてよい部分。ここも人の判断推奨 |
| LINEチャットのタグ付与機能 | 「LINEでのやり取りに『対応状況』『担当者』のタグを付けて管理できるようになりました」 | LINEはすでに使っている前提（先方課題にLINE費用の話がある）なので、機能名・タグの種類まで具体的に書いて問題ないと判断 | 　 |

### 共有させていただきたいアップデート内容

▼共有させていただきたいアップデート内容
- 最後の来院から一定期間が経過した患者様への声かけ、自動でできるようになりました
- 患者様の状態に応じて、配信メッセージを自動で変えられるようになりました
- マイページから複数のコースを同時に予約できるようになりました
- 友達紹介をきっかけに、紹介者様へポイントが付与される仕組みが追加されました
- LINEでのやり取りに「対応状況」「担当者」のタグを付けて管理できるようになりました

---

## 模範解答（大翔会 gift clinic・担当変更後の初回定例）

※「派生パターン：担当変更後の初回定例（フル文面）」の型確認用。タイトル化メモは作らず、
この1通のみを送る。

```
大翔会 gift clinic
武田様
いつも大変お世話になっております。
改めて担当変更をさせていただいた佐久間と申します。
引き続きどうぞよろしくお願い致します。

さて、直近のメディカルフォースのアップデートで、貴院にお役立ていただけそうな内容がございましたので、オンラインMTGにて引き継ぎ後のご挨拶も兼ねてお時間いただければと思いご連絡をさせていただきました。
すでに共有済みの機能もあるかもしれませんが、特に下記は前回の定例でお伺いした内容に関連する可能性があり、定例で詳しくお伝えできればと考えております。

▼共有させていただきたいアップデート内容
- ポイントの付与・利用をAPI経由で操作できるようになりました
- 前受金の受け取り時ではなく、実際のご利用に充当されたタイミングでポイントが付与されるよう仕様が変更されました
- 来院者様の診察券QRコードをAPI経由で取得できるようになりました
このほか、症例写真や書類をまとめて別のフォルダへ移動できるようになるなど、業務全般で使える改善も入っております。

また近頃、他院様より、電子カルテの運用課題にとどまらず、経営・運営面でのお悩みをご相談いただく機会が増えてまいりました。
つきましては、改めてシステムのご活用状況をお伺いするとともに
御院の事業方針や経営・運営面における課題感についても深くお聞かせいただき
直近の売上状況などもデータでご共有しながら弊社としてより一層お役に立てるご提案ができればと考えております。

現在対応可能な日程として下記時間を確保いたしました。
ご多忙の折、恐縮ですがご都合はいかがでしょうか。

【候補日時】


※上記日程で差し支えなければ、Google meetのURLをお送りいたします。
もしご都合がつかない場合は、大変お手数ですが候補日を2〜3ほどいただけますと幸いです
```

### この実例での判断ポイント

- 担当変更の挨拶は「オンラインMTGにて引き継ぎ後のご挨拶も兼ねて」の1箇所にまとめ、自己紹介の文とは別に重ねて書かない
- 課題ひも付けのある3件（ポイント付与・使用API／前受金ポイント付与タイミング変更／診察券QRコードAPI）と、課題ひも付けのない汎用アップデート（症例写真・書類の一括移動）を「このほか〜」で明確に切り分けた。全件を「前回の定例でお伺いした内容に関連する可能性がある」で束ねると、汎用アップデートについては事実と異なる印象を与えるため
- 経営相談への展開（「また近頃、他院様より〜」以降）は、担当変更というタイミングだからこそ違和感なく入れられる要素として追加。プロダクトアップデートの共有だけに閉じず、関係構築・アップセルの糸口を作る狙い
- 日程候補は空欄のままでよい（CS担当が個別に埋める）

---

## 注意事項

- **このスキルはAI→CS担当の順で動く。最終判断は常に人（CS担当）が行う。**
- 確定・候補・対象外・汎用アップデート、すべてを省略せず毎回CS担当が確認する（1件ずつではなく全件まとめて）。
- 「案内済み」の判断はSalesforceのMTGメモの記載を信頼し、専用の管理ログは作らない。
- Salesforceの契約状況（Account.利用機能・Opportunity履歴）が取得できる環境（Claude Code等）では、必ずマッチング処理の前に確認する。取得できない環境（claude.aiプロジェクト等）ではこのステップを省略し、MTGメモのみでの照合に戻ってよい。
- Account.利用機能（`Function__c`）は商談受注時点で更新される運用であり、実際の利用開始日（Opportunityの`変更適応日`）とはズレることがある。「契約済み・利用開始待ち」と「現在運用中」は案内文言上区別する。
- クリニックごとの契約状況をスナップショットとしてファイル保存し使い回す運用は、現時点では未着手（都度SOQLで確認する）。必要になった時点で改めて検討する。
- 模範解答はあくまで参考。顧客・MTGの内容によって粒度・抽象度は都度変わる。
- スキルの改善・更新は `shojisakuma-bot/update-delivery-suggester` リポジトリで管理する。
